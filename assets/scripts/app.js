let currentResult = 0;

const description = `((${currentResult} + 2) / 2) + 10`;

currentResult = (currentResult + 2) / 2 + 10;

outputResult(currentResult, description);
outputResult(description, currentResult);
